//! The client for a service whose every operation expects no reply. The types the author declared
//! are named one by one, which is what the expansion spells them as.

use crate::tests::AnnotationRequest;

note_service_amqp_rpc_client!();
