//! The second service's dispatcher, expanded out of its own macro into a module of its own — the
//! same crate carrying two of them without either naming the other's module.

enrol_service_amqp_rpc_dispatcher!();
