//! The AMQP request-and-reply transport: what a service asking for `amqp_rpc` gets.
//!
//! Empty for now. The dispatcher and the client this transport contributes are moved here by their
//! own tasks; what already exists is the name, bound in the registry
//! [`transport`](super) beside every other.
